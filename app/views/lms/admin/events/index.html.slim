.flex.items-center.justify-between
  h3.text-lg.font-medium Courses
  .breadcrumbs.hidden.p-0.text-sm class='sm:inline'
    ul
      li LMS
      li
        = link_to_unless_current "Courses", lms.admin_courses_path

.mt-6 
  .bg-base-100.card.shadow 
    .card-body.p-0 
      .flex.items-center.justify-between.px-5.pt-5 
        .inline-flex.items-center.gap-3 
          label.input.input-sm
            span.iconify.lucide--search.size-3.5.text-base-content/80
            input.w-24 class="sm:w-36" placeholder='Search courses...' aria-label='Search courses' type='search'
        .inline-flex.items-center.gap-3
          = link_to lms.new_admin_course_path, class: 'btn btn-primary btn-sm max-sm:btn-square', html: { "aria-label": "Create course link"} do
            span.iconify.lucide--plus.size-4
            span.hidden class='sm:inline' New Course
      .mt-4.overflow-auto 
        table.table 
          thead
            tr
              th
                input.checkbox.checkbox-sm type='checkbox' aria-label='Check all'
              th ID
              th Title
              th Action
          tbody 
            - @courses.each do |course|
              tr.hover:bg-base-200/40.cursor-pointer class='*:text-nowrap'
                th
                  input.checkbox.checkbox-sm type='checkbox' aria-label='Single check'
                td = course.id
                td = course.title
                td
                  .inline-flex 
                    = link_to lms.edit_admin_course_path(course), class: 'btn btn-square btn-ghost btn-sm', html: { "aria-label": "Edit course link" } do
                      span.iconify.lucide--pencil.text-base-content/80.size-4
                    = link_to lms.admin_course_path(course), class: 'btn btn-square btn-ghost btn-sm', html: { "aria-label": "Show course link" } do
                      span.iconify.lucide--eye.text-base-content/80.size-4
                    = button_to lms.admin_course_path(course), class: 'btn btn-square btn-error btn-outline btn-sm border-transparent', method: :delete, data: { confirm: 'Are you sure?' } do
                      span.iconify.lucide--trash.size-4